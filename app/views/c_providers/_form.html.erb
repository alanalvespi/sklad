<%= form_for(@c_provider) do |f| %>
  <% if @c_provider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@c_provider.errors.count, "error") %> prohibited this c_provider from being saved:</h2>

      <ul>
      <% @c_provider.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name_provider %><br />
    <%= f.text_field :name_provider %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
