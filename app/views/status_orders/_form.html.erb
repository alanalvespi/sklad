<%= form_for(@status_order) do |f| %>
  <% if @status_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status_order.errors.count, "error") %> prohibited this status_order from being saved:</h2>

      <ul>
      <% @status_order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_status %><br />
    <%= f.number_field :id_status %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :notes %><br />
    <%= f.text_field :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
